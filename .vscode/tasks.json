{
  "tasks": [
    {
      "type": "shell",
      "label": "g++-7 build active file",
      "command": "/usr/bin/g++-7",
      "args": [
        "-g",
        "${fileDirname}/../main.cpp",
        "${fileDirname}/../ConfigReader.cpp",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}",
        "-lzmq"
      ],
      "options": {
        "cwd": "/usr/bin"
      }
    }
  ],
  "version": "2.0.0"
}