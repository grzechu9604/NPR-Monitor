{
  "tasks": [
    {
      "type": "shell",
      "label": "g++-7 build active file",
      "command": "/usr/bin/g++-7",
      "args": [
        "-g",
        "${workspaceFolder}/main.cpp",
        "${workspaceFolder}/ConfigReader.cpp",
        "${workspaceFolder}/MessageSender.cpp",
        "-o",
        "${workspaceFolder}/${fileBasenameNoExtension}",
        "-lzmq"
      ],
      "options": {
        "cwd": "/usr/bin"
      }
    }
  ],
  "version": "2.0.0"
}